# Khet 雷射棋 — 桌面版依賴（Python 3.10）
# 安裝：.\venv\Scripts\python.exe -m pip install -r requirements.txt
PyQt6==6.7.1
pytest==8.3.3

# 打包（Phase 5 才需要，先裝好無妨）
pyinstaller==6.11.1
pillow==11.0.0          # 僅打包時用：assets/app_icon.png → .ico
